class Solution(object):
    def fib(self, n):
        """
        :type n: int
        :rtype: int
        """
        # write base case
        if n < 2:
            return n
        else:
            return self.fib(n-1) + self.fib(n-2)

        # class self format
        # TODO: Solve with memoization 
        
        
